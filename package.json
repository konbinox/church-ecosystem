# Church Ecosystem 🌟

A decentralized, AI-powered micro-church community network built on modern Web3 stack.

## Quick Start
```bash
npm install
npm run dev

**2. 创建 package.json**
```bash
@'
{
  "name": "church-ecosystem",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.10.0",
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "frontend/apps/*",
    "frontend/packages/*",
    "protocol/contracts", 
    "services/*"
  ]
}
